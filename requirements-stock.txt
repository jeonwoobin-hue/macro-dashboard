# run_stock_pipeline.py(종목 심리분석 배치 데이터 갱신, stockanalyzer.main.run_pipeline 사용) 전용 의존성.
# kiwipiepy는 "전체 기능 이식" 이후 실시간 검색·비교·업종분석 기능 때문에 메인
# requirements.txt에도 이미 포함돼 있지만, matplotlib(stockanalyzer/report.py의 차트 생성)은
# 배포 런타임에 절대 넣지 않는다 — 이 프로젝트에서 과거 세그폴트를 일으켰던 것과 같은 카테고리의
# 네이티브 패키지라, 이 스크립트를 로컬/GitHub Actions에서 실행할 때만 설치한다.
requests
beautifulsoup4
lxml
pandas
matplotlib
kiwipiepy
